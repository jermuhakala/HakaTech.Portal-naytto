@model HakaTech.Portal.Models.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Kirjaudu sisään";
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
}

<div class="auth-card">
    <div class="auth-logo">
        <img src="~/img/logo.png" alt="HakaTech Logo" style="height: 48px; width: 48px; border-radius: 12px; object-fit: cover;" />
        <span>HakaTech Portal</span>
    </div>
    <h1 class="auth-title">Kirjaudu sisään</h1>
    <p class="auth-sub">Syötä tunnuksesi jatkaaksesi</p>

    <form asp-action="Login" method="post">
        <input type="hidden" asp-for="ReturnUrl" />
        <div asp-validation-summary="ModelOnly" class="alert-auth-error" role="alert"></div>

        <div class="auth-field">
            <label asp-for="Email" class="auth-label"></label>
            <div class="auth-input-wrap">
                <i class="bi bi-envelope auth-input-icon"></i>
                <input asp-for="Email" class="auth-input" placeholder="esimerkki@yritys.fi"
                       id="login-email" autocomplete="username" />
            </div>
            <span asp-validation-for="Email" class="field-validation-error"></span>
        </div>

        <div class="auth-field">
            <label asp-for="Password" class="auth-label"></label>
            <div class="auth-input-wrap">
                <i class="bi bi-lock auth-input-icon"></i>
                <input asp-for="Password" class="auth-input" placeholder="••••••••"
                       id="login-password" autocomplete="current-password" />
            </div>
            <span asp-validation-for="Password" class="field-validation-error"></span>
        </div>

        <div class="auth-check">
            <input asp-for="RememberMe" class="auth-checkbox" id="login-remember" />
            <label asp-for="RememberMe" class="auth-check-label">Muista minut</label>
        </div>

        <button type="submit" id="login-submit" class="auth-btn">
            <i class="bi bi-box-arrow-in-right me-2"></i>Kirjaudu sisään
        </button>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
